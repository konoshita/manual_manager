<% content_for(:title, t('.title')) %>
<% breadcrumb :manuals_path %>
    <!-- 掲示板一覧 -->
<div class="container">
  <div class="section-block-grey">
  <h3><%= t('.title') %></h3>
      <div class="row row-cols-1 mb-3 ">
      <%= render 'search_form', url: manuals_path %>
      <ul class="fusen">
        <% if @manuals.present? %>
          <%= render @manuals %>
        <% else %>
          <%= render 'noresult' %>
        <% end %>
      </ul>
    </div>
  </div>