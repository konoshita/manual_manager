<section class="content-header">
  <div class="container-fluid">
    <div class="row mb-2">
      <div class="col-sm-6">
      <h1><%= t('.title') %></h1>
      </div>
      <div class="col-sm-6">
      <% breadcrumb :admin_manuals %>
      </div>
    </div>
  </div><!-- /.container-fluid -->
</section>
<div class="container mb-5 pt-2">
  <!-- /.row -->
  <div class="row">
    <div class="col-12">
      <div class="box box-primary">
        
        <!-- /.card-header -->
        <div class="box-body table-responsive no-padding" style="height: 90vh;">
          <table class="table table-hover table-head-fixed text-nowrap">
          <thead>
          <tr>
            <th scope="col"><%= Manual.human_attribute_name(:id) %></th>
            <th scope="col"><%= Manual.human_attribute_name(:title) %></th>
            <th scope="col"><%= Manual.human_attribute_name(:user_id) %></th>
            <th scope="col"><%= Manual.human_attribute_name(:created_at) %></th>
            <th scope="col"></th>
            <th scope="col"></th>
            <th scope="col"></th>
          </tr>
          </thead>
          <tbody>
          <% if @manuals.present? %>
            <%= render @manuals %>
          <% else %>
            <p><%= t('.no_result') %></p>
          <% end %>
          </tbody>
          </table>
        </div>
        <!-- /.card-body -->
      </div>
      <!-- /.card -->
    </div>
    <div class="row">
    <div class="col-md-12">
      <!-- ページネーション -->
      
    </div>
  </div>
  </div>
  <!-- /.row -->
