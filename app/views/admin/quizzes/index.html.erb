<% content_for(:title, t('.title')) %>

<% breadcrumb :admin_quizzes %>
<div class="col-md-12">
  <div class="card card-primary card-outline">
      <div class="container mb-5 pt-2">
        <div class="row">
          <div class="col">
            <div class="box box-primary">
              <div class="box-body table-responsive no-padding" style="height: 90vh;">
                <table class="table table-hover table-head-fixed text-nowrap">
                  <thead>
                    <tr>
                      <th scope="col"><%= Quiz.human_attribute_name(:id) %></th>
                      <th scope="col"><%= Quiz.human_attribute_name(:title) %></th>
                      <th scope="col"><%= Quiz.human_attribute_name(:created_at) %></th>
                      <th scope="col"></th>
                      <th scope="col"></th>
                      <th scope="col"></th>
                    </tr>
                  </thead>
                  <tbody>
                    <% if @quizzes.present? %>
                      <%= render @quizzes %>
                    <% else %>
                      <p><%= t('.no_result') %></p>
                    <% end %>
                  </tbody>
                </table>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
    <div class="row">
      <div class="col-md-12">
        <!-- ページネーション -->
    </div>
  </div>
  <!-- /.row -->
</div>